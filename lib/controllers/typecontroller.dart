class TypeController {
  // Defina as propriedades e métodos necessários para controlar o objeto

  late Type type;

  TypeController(this.type);

  void atualizarObjeo(Type) {
    // Lógica para atualizar o objeto
    this.type = Type;
  }
}
